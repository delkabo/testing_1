from pathlib import Path
import os
from tkinter import *
from tkinter import ttk
# from tkinter.ttk import Combobox


p = Path(__file__).parents[3]
list = os.listdir(p)
print(f"hello {list}")

# def clicked():
#     lbl.configure(text=f"hello {list}")

pairs = [("name", "Anna"), ("age", 28), ("city", "berlin")]
person = dict(pairs)

xixi1 = 0
lbl2 = ""
def clicked():
    global xixi1
    # xixi1 = xixi1 + 1
    # lbl.configure(text=f"hello {pairs}")
    # lbl2 = Label(window, text=f"Текст появился {xixi}")
    # lbl2.grid(column=0, row=1)
    global lbl2
    if xixi1 == 0:
        xixi1 = 1
        lbl2 = Label(window, text="Текст появился")
        lbl2.grid(column=0, row=1)
        print(f"xixi {xixi1}")
    else:
        xixi1 = 0
        # lbl2 = Label(window, text="Текст появился")
        # lbl2.grid(column=0, row=1)
        if lbl2.winfo_exists():
            print("Кнопка lbl2 существует")
            lbl2.destroy()
        print(f"xixi {xixi1}")    

window = Tk()
style = ttk.Style()
style.configure("TButton", foreground="black", background="blue", font=("Arial",12), padding=10)
window.title("Добро пожаловать")
window.geometry('500x200')
lbl = Label(window, text="Пример текста", font=("Arial Bold", 15))
lbl.grid(column=0, row=0)
# lbl2 = Label(window, text=f"Текст появился {xixi1}")
# lbl2.grid(column=0, row=1)
# lbl = Label(window, text=list, font=("Arial Bold", 15))
# lbl.grid(column=0, row=0)
btn = ttk.Button(window, text="dont push", style="TButton", command=clicked)
btn.grid(column=2, row=0)
combo=ttk.Combobox(window)
combo['values'] = (1,2,3,4,5,"Привет")
combo.current(1)
combo.grid(column=1, row=0)
window.mainloop()

print(person)


# answ = input("Хотите ли вы получить все данные: ")
# if answ == "да":
#     print(person)
